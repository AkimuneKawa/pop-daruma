# ポップだるま堂 繁盛記

スマホ縦画面・1画面完結のリアルタイム工房経営シミュレーション（昭和レトロな町のだるま工房）。
仕様の詳細・経緯は `docs/HANDOFF.md` を参照（ゲーム仕様＝第3章、UI＝第4章、シーン描画＝第5章）。

## 守るべき原則
- **スクロールなしで1画面に収めること**（最重要）。390×844 と 375×667 で必ず確認する
- 操作は5種類：作る色・素材を買う・投資・宣伝・速度
- ミニゲームや品質の概念は入れない（意図的に廃止済み）
- トーンは昭和レトロポップ × SFC風ドット絵。作る色は全職人共通で1色

## コマンド
- `npm run dev` 開発サーバー（http://localhost:5173）
- `npm test` ユニットテスト＋自動プレイのバランス検証（vitest）
- `npm run autoplay -- 100` 自動プレイの統計を表示
- `npm run e2e` Playwright で2サイズのはみ出しチェック。スクショは `e2e/screenshots/`
- `npm run build` 本番ビルド（`dist/`、相対パスなのでどこに置いても動く）

## 構成
| ファイル | 役割 |
|---|---|
| `src/constants.js` | バランス調整値（`MM, BASE, MAT, STAFF, RACK_UP, WH_UP` など） |
| `src/sim.js` | シミュレーション本体。**DOM 非依存**。状態 `S` を引数で受け取り、画面への通知は `hooks`（sale/news/save/short/end）で行う。乱数は `rng` 引数で差し替え可 |
| `src/ui.js` | パネル表示（`renderUI`）、モーダル、トースト |
| `src/scene.js` | 工房シーンの canvas 描画と来客の動き |
| `src/sprites.js` | ドット絵データ（文字列配列）と `spr` / `darPal` |
| `src/events.js` | 季節イベント・バズ・事件の定義（効果・ニュース文・町の空気・旗飾り・客の国籍）と `genEvents` |
| `src/roster.js` | 職人名簿（100人）と月給の式 `wageOf` |
| `src/audio.js` | BGM と効果音（Web Audio で合成。音声ファイルなし）。売れたときの「チャリーン」、年末商戦はお祭りの曲。音の設定は localStorage の `popdaruma_sound` |
| `src/save.js` | localStorage のセーブ／ロード |
| `src/main.js` | 起動、ゲームループ、操作ダイアログの配線 |
| `scripts/autoplay.js` | 自動プレイ（投資なし `passive`／投資あり `active`） |
| `legacy/pop-daruma.html` | 移植元の単一HTML（見た目・挙動の比較用。編集しない） |

## 注意
- UI の寸法はすべて rem（ルート font-size は `clamp(9px, min(1.72dvh, 3.05vw), 16px)`）。px を持ち込まない
- 時間：1ステップ＝1週（実時間 `STEP_SEC`＝15秒）。1ヶ月＝4週（`WEEKS`）、1年＝48週（`TOTAL`、4月第1週〜3月第4週）。乾燥1週（`DRY`）、入金は翌週（`PAY_DELAY`）。家賃・給料は4週ごとの月末払い
- 規模：`QTY`（=140）が1週あたりの量の単位（生産・需要・仕入れ・入荷上限）。在庫・容量（`START_*`・`RACK_*`・`WH_*`）は別に決めている。売値3,000円・素材1,100円。家賃50万円・給料・投資・宣伝の金額は月単位のまま。上手なプレイで年商1億円前後
- 客は1人ずつ来る（刻みごとにポアソン分布で人数を抽選）。1人が欲しい個数は `BUYERS`（ふつう1〜3個・小売店5〜30個・業者50〜100個）。在庫が足りなければあるだけ買い、残りは売り逃し
- 人気（`S.pop`、`POP`）：欲しい分を全部買えた客で上がり（ふつう+1・小売店+3・業者+8、職人の腕前で倍率）、何も買えなかった客で−0.5。通常の客足（特需を除く）は人気に応じて `minMult`〜`maxMult` 倍。パネルではゲージ（5区切り・Lv1〜5）で見せ、上がると光って▲、Lv が上がるとトーストと効果音
- 宣伝（`ADS`、`S.ads`）：操作行の「宣伝」からチラシ・SNS広告・テレビCM。お金を払ってすぐ人気を上げ、1〜2週間は通常の客足が増える。同じ宣伝は効果が切れるまで重ねられない。テレビCM中は工房のテレビにCMが映る
- 職人（`CRAFT`、`src/roster.js`）：固定シードで作る100人の名簿。各自「うまさ」「素早さ」★1〜5を持ち、月給は両方が高いほど高い（±10%の個人差）。素早さ＝生産（★1あたり `ratePerSpeed`）、うまさ＝満足した客で上がる人気の倍率（工房全体の腕前＝作る量で重み付けした平均、本人★2で1倍）。求職者4人が毎週入れ替わり、最大6人雇える。「やめてもらう」で解雇可（契約金は戻らない）。雇った職人は `S.staff` に名簿から写したオブジェクトで持つ
- 乾燥棚は8段階・倉庫は6段階まで増強できる（`RACK_UP`・`WH_UP` の配列の長さが段階数）
- だるまは5色（あか・きん・ピンク・あお・みどり、キー red/gold/pink/sky/green）。きんは売値1.6倍。色ごとの需要の割合は `SHARE`（通常月・年末商戦）
- 暦（`MM`・`RUSH`、週で指定）：4月は桜、10月後半（26週）から素材高、11〜12月（28〜35週）が年末商戦（需要最大・売値×1.4・素材の入荷が細る・求職者なし）、1月は年明けで客足が急減、2月は春節
- イベント（`src/events.js` の `EVENT_TYPES`・`genEvents`）：季節（桜・春節）、バズ3回（テレビ特集／SNS、告知の翌週から2週間）、事件2回（台風＝入荷停止／原材料高騰／ホルムズ海峡封鎖＝きんが作れない）。効果・ニュース文・町の空気・旗飾り・客の国籍をここで定義する
- 客の国籍（jp/cn/west）：普段は `ORIGIN_BASE`、イベントで増えた客はイベントの `origin`。見た目（中国客＝赤や金の服・ツアー旗、欧米客＝明るい髪・リュック）と吹き出しの言葉が変わる
- 工房シーンのだるま1体＝`DRAW_UNIT`（10個）。乾燥棚だけは小さいだるま72体に収まるよう、容量に応じて1体あたりの個数を増やす。客は1人＝1人で描く（画面上は最大24人）
- 乾燥棚は `{c, n, ready}` の束で持つ
- 表示：金額は `yen()` のカンマ区切り（例：2,400,000円）。所持金は桁数に応じて文字を小さくする。個数は `cnt()`（例：7,200／2.4万）
- 客の吹き出し：向かってくる客は「欲しい色のだるま×個数」、店先の客は結果（買った！／売り切れ…）。2段に重ならないよう配置
- セーブキーは `popdaruma_rt_v5`。v1〜v4 のセーブは `save.js` の `migrate`（`OLD_SAVES` の倍率で v4 の規模にそろえる）→ `weekify`（日→週、数量と人気を半分）で変換して読み込む。`S` の構造を変えるときはキーを上げるか変換処理を追加する
- 色キー `sky` は表示名が「あお」でもセーブ互換のため変えない
- バランスを変えたら `npm run autoplay -- 200` で次を確認する：投資なし（あかだけ作る）＝一人前9割・見習い1割、人気は年末までにLv3〜5。投資あり（職人・倉庫へ投資し年末商戦に備蓄、在庫に余裕があれば宣伝）＝ほぼ一人前・年商 中央値約1億（6割ほどが1億達成）・人気Lv5は8月ごろ
- 人気の上がり方は厳しくしない（オーナー判断）。難易度は素材価格・家賃・給料・称号の基準で調整する
- 見た目を変えたら `npm run e2e` を実行し、スクショを目で確認する

## デプロイ
- 公開URL：https://akimunekawa.github.io/pop-daruma/ （GitHub Pages）
- `main` に push すると `.github/workflows/deploy.yml` がテスト→ビルド→公開まで自動で行う。テストが落ちたら公開されない
